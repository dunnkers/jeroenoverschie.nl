{
    "scripts": {
        "build": "gssg && replace-in-file --configFile=replace-in-file_config.js",
        "predeploy": "echo 'jeroenoverschie.nl' > static/CNAME",
        "deploy": "gh-pages -d static"
    }
}